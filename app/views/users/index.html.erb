<h1>Our Hungry Hungry Users...</h1>

<% @users.each do |user| %>
	<div class="users">
    <h3>
    <%= gravatar_for user %>
      <%= link_to user.name, user_path(user) %>
    </h3>
    <h4>
    <%= user.username %>
    </h4>
    <p><em>
      <%= user.email %>
    </em></p>
  	</div>
<% end %>